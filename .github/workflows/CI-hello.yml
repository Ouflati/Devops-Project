name: CI Hello Calendar

on:
  push:
    branches:
      - feature/calendar
  pull_request:
    branches:
      - feature/calendar

  # Lancement manuel
  workflow_dispatch:

jobs:
  hello-ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Say Hello
        run: |
          echo "Hello World👋"
          echo "Branch: feature/calendar"
          echo "Workflow CI is running successfully"
