
[200~name: Hello World

on:
  push:
    branches:
      - "Team6/profile-features"
  workflow_dispatch:

jobs:
  say-hello-inline-bash:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "hello from an inline bash script in a GitHub Action Workflow!"
